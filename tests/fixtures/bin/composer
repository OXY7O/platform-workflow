#!/usr/bin/env bash
set -euo pipefail
if [[ " $* " == *" audit "* && "${COMPOSER_FAKE_AUDIT_FAILURE:-}" == "1" ]]; then exit 1; fi
if [[ " $* " == *" test:phpunit "* && "$PWD" == *"laravel-test-failure"* ]]; then exit 1; fi
exit 0
