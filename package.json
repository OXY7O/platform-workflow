{
  "name": "@oxy7o/platform-workflow",
  "version": "0.7.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bundle:all": "npm run build && ncc build lib/action-validate-contract.js -o dist/validate-contract && ncc build lib/action-validate-compatibility-contract.js -o dist/validate-compatibility-contract && ncc build lib/action-generate-compatibility-matrix.js -o dist/generate-compatibility-matrix && ncc build lib/action-normalize-results.js -o dist/normalize-results && ncc build lib/action-build-artifact.js -o dist/build-artifact && ncc build lib/action-validate-go-service-contract.js -o dist/validate-go-service-contract && ncc build lib/action-build-go-binary-artifact.js -o dist/build-go-binary-artifact && ncc build lib/action-validate-go-compatibility-contract.js -o dist/validate-go-compatibility-contract && ncc build lib/action-generate-go-compatibility-matrix.js -o dist/generate-go-compatibility-matrix && ncc build lib/action-validate-dotnet-webapi-contract.js -o dist/validate-dotnet-webapi-contract && ncc build lib/action-build-dotnet-application-artifact.js -o dist/build-dotnet-application-artifact && ncc build lib/action-validate-dotnet-compatibility-contract.js -o dist/validate-dotnet-compatibility-contract && ncc build lib/action-generate-dotnet-compatibility-matrix.js -o dist/generate-dotnet-compatibility-matrix && ncc build lib/action-validate-oci-build.js -o dist/validate-oci-build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src tests scripts/*.mjs",
    "validate:public-boundary": "node scripts/validate-public-execution-boundary.mjs",
    "validate:repository": "node scripts/validate-repository.mjs && npm run validate:public-boundary",
    "test": "npm run build && node --test tests/**/*.test.mjs"
  },
  "dependencies": {
    "@actions/core": "3.0.1",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.4.0",
    "@vercel/ncc": "0.45.0",
    "eslint": "10.9.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.68.0"
  }
}
